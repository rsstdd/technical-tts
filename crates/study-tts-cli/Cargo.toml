[package]
name = "study-tts-cli"
version = "0.1.0"
edition = "2024"
license.workspace = true
rust-version.workspace = true

[lints]
workspace = true

# Candidates in ADR-0001 §7.2 are declared when the story that needs
# them lands, not in advance: an unused dependency is still audited,
# licensed, and carried in Cargo.lock.
#
# This crate reports E1-S1 status until product commands land at E1-S5, so it
# has no dependencies at all — not even the workspace crates.
[dependencies]
