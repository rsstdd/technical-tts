//! Placeholder binary for the E0-S0 walking skeleton.
//!
//! ADR-0001 §1 puts the product CLI at G1; until then the tested library is the deliverable and
//! this exists so the workspace builds a binary target.

fn main() {
    println!(
        "study-tts E0-S0: the tested library walking skeleton is available; product CLI commands begin at G1"
    );
}
