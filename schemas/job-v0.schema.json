{
  "$defs": {
    "ManifestDigest": {
      "description": "BLAKE3 over an immutable package manifest's bytes, as 64 lowercase hexadecimal characters.",
      "pattern": "^[0-9a-f]{64}$(?![\\s\\S])",
      "type": "string"
    },
    "PlanHash": {
      "description": "BLAKE3 over the canonical serialization of a render plan's resolved segments, as 64 lowercase hexadecimal characters.",
      "pattern": "^[0-9a-f]{64}$(?![\\s\\S])",
      "type": "string"
    },
    "ProvisionalJobStage": {
      "description": "The E0 progress stage a snapshot records, not the complete E2 state machine.",
      "oneOf": [
        {
          "const": "planned",
          "description": "A validated lesson has a deterministic render plan.",
          "type": "string"
        },
        {
          "const": "caching",
          "description": "Cache lookup and staged-audio publication are in progress.",
          "type": "string"
        },
        {
          "const": "packaging",
          "description": "Validated cached artifacts are ready for package writing.",
          "type": "string"
        },
        {
          "const": "package_selected",
          "description": "An immutable package has been selected for preview consumers.",
          "type": "string"
        }
      ]
    },
    "SelectedPackageIdentity": {
      "additionalProperties": false,
      "description": "Immutable package identity retained in the selected job state.\n\nThe two fields carry one value today. `study_tts_runtime::package_port`\nnames a package directory by the BLAKE3 of the `manifest.json` inside it, so\nthe identity that resolves the directory and the identity that verifies the\nmanifest are the same digest, and nothing in this type holds them equal — a\nreader that needs them to agree must compare them. Whether the duplication\nsurvives is a G1 freeze question: collapsing it is a breaking change to the\npublished `job-v0.schema.json` under\n`docs/governance/INTERFACE-FREEZE-AND-CHANGE-CONTROL.md`, not an edit.",
      "properties": {
        "manifest_blake3": {
          "$ref": "#/$defs/ManifestDigest",
          "description": "BLAKE3 digest of the selected package manifest."
        },
        "package_id": {
          "$ref": "#/$defs/ManifestDigest",
          "description": "Content identity naming the immutable package directory."
        }
      },
      "required": [
        "package_id",
        "manifest_blake3"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.study-tts.example/job-v0.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Minimal durable snapshot replaced atomically by the runtime repository port.",
  "properties": {
    "job_id": {
      "description": "E0 ownership identity, currently the validated lesson identifier.",
      "type": "string"
    },
    "plan_hash": {
      "$ref": "#/$defs/PlanHash",
      "description": "Hash of the deterministic render plan."
    },
    "schema_version": {
      "const": "e0.job-state.0.1",
      "description": "Provisional schema version required at the repository boundary.",
      "type": "string"
    },
    "selected_package": {
      "anyOf": [
        {
          "$ref": "#/$defs/SelectedPackageIdentity"
        },
        {
          "type": "null"
        }
      ],
      "description": "Selected package identity, present only in `package_selected`."
    },
    "stage": {
      "$ref": "#/$defs/ProvisionalJobStage",
      "description": "Last durable E0 stage."
    }
  },
  "required": [
    "schema_version",
    "job_id",
    "plan_hash",
    "stage"
  ],
  "title": "ProvisionalJobSnapshot",
  "type": "object"
}
